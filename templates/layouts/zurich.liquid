<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Café Zurich{% endblock %}</title>
        {{ "app.css" | asset_url | stylesheet_tag }}
        <!-- TODO: Work out template architecture :) -->
        <!-- TODO: Save the architecture as a boilerplate to mixture :) -->
        <!-- TODO: Remember: includes for components, blocks for main content -->
    </head>
    <body class="wrapper">
        {% include "sliding-nav" %}
        <div class="container">
            {% block content %}{% endblock %}
        </div>
        {% include "footer" %}
        <script src="http://code.jquery.com/jquery-2.1.1.min.js"></script>
        {{ "sliding-nav.js" | asset_url | script_tag }}
    </body>
</html>